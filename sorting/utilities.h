#ifndef UTILITIES
#define UTILITIES

void swap(int* a, int* b);

unsigned int getSize(); // take array size input
int* InputArray(unsigned int size); // take array input and return it
void PrintArray(int* A, unsigned int N); // print the given array

#endif
